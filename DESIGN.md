# 몰입캘린더 — Design System

## v0.2 — 몽환 파스텔 픽셀아트 (현재 방향)

레퍼런스 스크린샷 기반으로 스타일을 전환했다. 아래 v0.1(TickTick 저자극) 섹션의 원칙 중 **"한눈에 파악", "색 = 상태 정보", "모션 최소화(배경 데코 제외)"는 유지**하고, 표현만 바꿨다.

- **분위기**: 라벤더→핑크 밤하늘 그라디언트 + 픽셀 별/달/구름. 앱을 "해야 할 일 목록"이 아니라 "매일 돌아오고 싶은 공간"으로 만들기 위한 선택 — ADHD 사용자에게는 도구의 정서적 매력이 지속 사용의 핵심 변수.
- **폰트**: Galmuri11(본문) + DungGeunMo(디스플레이). 픽셀 폰트가 레트로 게임 감성을 만들고, XP/스트릭 게이미피케이션과 톤이 일치.
- **팔레트**: 전 카테고리 파스텔(채도 낮음) — 색 수는 많지만 명도가 균일해 시각적 소음이 낮다. 진한 색은 `accent`(#8b7cf6) 하나뿐: 선택 상태/CTA 전용.
- **서피스**: 흰 패널 + 2px 라운드 보더 + 오프셋 그림자(픽셀 버튼 느낌). 다크모드는 v0.2에서 제외(밤하늘 배경이 이미 중간 명도).
- **산출물**: `index.html`(동작하는 앱, localStorage 저장), `design-preview.html`(쇼케이스 포스터 목업), `design-tokens.json`(v0.2.0 토큰).

---

## v0.1 — TickTick 저자극 (아카이브)

TickTick을 모티브로 한, 개발자 + ADHD 사용자를 위한 통합 일정관리 앱의 디자인 시스템.
macOS / iPhone / Desktop(웹) 공용으로 쓸 것을 전제로, 시각적 과부하를 줄이고 "지금 뭘 해야 하는지"를 1초 안에 파악하게 하는 것이 최우선 목표.

## 왜 이런 선택을 했는가

### ADHD 친화 원칙 (전체를 관통하는 기준)
- **저자극 기본값**: 채도 높은 색은 태그/상태 표시에만 쓰고, 배경·surface는 무채색 계열. 색이 전부 튀면 "중요한 것"이 안 보임.
- **모션 최소화**: 애니메이션은 120–200ms, ease-out만 사용. 바운스·반짝임 금지 — 화면이 계속 움직이면 ADHD 사용자는 산만해짐.
- **한눈에 파악**: 텍스트보다 색+아이콘 조합으로 상태 전달 (완료/진행중/놓침). 읽기보다 보기.

### 색상 (color)
- `accent`(#4C6EF5, 차분한 블루) — 기본 인터랙션/스케줄. TickTick의 레드는 그대로 쓰지 않음: 하루종일 보는 화면에 레드가 기본색이면 지속적 긴장감을 유발.
- `warm`(#FF7849, TickTick 오렌지 계승) — 10분 타임트래커 전용. "지금 몰입 중"을 나타내는 색은 accent와 분리해야 트래커가 눈에 띔.
- `alert`(#F03E3E) — **약 복용 등 루틴 알람 전용**. 앱 전체에서 이 색이 나오면 무조건 "지금 챙겨야 하는 것"이라는 규칙을 고정. 색의 의미를 절대 다른 곳에 재사용하지 않음 (일관성이 신뢰를 만듦).
- `ai`(#7C5CFC, 퍼플) — **"Claude 코워크가 모아준 루틴"** 전용 색. 사람이 직접 만든 일정과 AI가 제안/수집한 항목을 시각적으로 구분해야 사용자가 "이건 내가 챙긴 게 아니라 자동으로 모인 것"임을 즉시 인지.
- `github`(#24292F, 뉴트럴 다크) — GitHub 프로젝트 카드 전용. 브랜드 인지도를 이용해 "이건 개발 프로젝트 영역"임을 설명 없이 전달.
- `tags`(9색 팔레트) — TickTick처럼 사용자가 자유롭게 태그에 색을 매핑. 프로젝트/컨텍스트별 구분용.

### 타이포그래피
- 시스템 폰트(SF Pro 계열) 기본 — 별도 웹폰트 로딩 없이 macOS/iOS 네이티브 느낌 유지, 성능도 최선.
- `font-mono`는 타임트래커의 시간 표기와 GitHub 카드의 커밋/브랜치명에만 사용 — "이 숫자/텍스트는 정확한 데이터"라는 신호를 개발자에게 줌.

### Spacing / Radius / Shadow
- 4px 배수 스케일 — 예측 가능한 리듬, 아무 값이나 쓰지 않음.
- Radius는 12–14px 중심의 부드러운 카드 — TickTick의 카드형 리스트 계승, 너무 둥글면(20px+) 장난감처럼 보여 개발자 도구 느낌이 옅어짐.
- Shadow는 sm/md만 기본 사용, lg는 모달 등 진짜 떠 있는 요소에만 — 그림자 남발은 시각적 노이즈.

## 화면 구성 (요구사항 매핑)

| 요구사항 | 화면/컴포넌트 |
|---|---|
| 1. 알람 기능 | 상단 배너 + 시스템 알림(macOS/iOS 네이티브 Notification 연동), `accent` 색 |
| 2. 약 복용 등 루틴 알람 | "루틴" 사이드바 섹션, `alert` 색 + 알약 아이콘, 체크 시 `success`로 전환 |
| 3. Claude 코워크가 모아주는 루틴 | "AI 루틴" 카드 그룹, `ai`(퍼플) 색으로 통일, 카드 우상단에 "Claude" 배지 |
| 4. GitHub 프로젝트 관리 | 사이드바 "프로젝트" 섹션, repo별 카드(오픈 PR/이슈 수), `github` 뉴트럴 톤 |
| 5. 스케줄 관리 | Month/Week 뷰, `accent` 기반 이벤트 블록 |

## 핵심 뷰

1. **월간 뷰(Month)** — TickTick처럼 날짜 셀 안에 태그 색 점(dot)만 표시, 텍스트 나열 없음 → 한눈에 밀도 파악.
2. **위클리 뷰(Week)** — 시간축 세로 그리드, 일정 블록에 태그 색 적용.
3. **10분 타임트래커** — 하루를 6칸/시간(10분 단위)으로 쪼갠 그리드. 칸을 클릭해 태그+메모로 기록 → 채워진 칸 비율로 "오늘 실제로 몰입한 시간"을 시각화. 이것이 이 앱의 핵심 차별점이므로 대시보드 최상단에 고정.
4. **루틴/알람 패널** — 약 복용(alert) / Claude 추천 루틴(ai) / 일반 습관(success)을 색으로 구분한 체크리스트.

## 원색 포인트 (Pop Accent)
새 색상을 추가하는 대신 기존 `warm`(#FF5722, 채도 높은 오렌지)을 뷰당 유일한 "원색 포인트"로 승격했다. FAB(할 일 추가), 활성 탭, 활성 네비게이션, 포커스 링에만 이 색을 쓴다.
- **이미 5개 시맨틱 색(blue/red/green/purple/dark)이 있는 상태에서 6번째 색을 더하면 팔레트가 산만해짐** — 대신 트래커/에너지와 맥락상 이미 연결된 `warm`을 진하게(#FF7849 → #FF5722) 끌어올려 재사용.
- 나머지 시맨틱 색(alert/ai/success/github)은 여전히 작은 dot·badge·border로만 등장 — "장식용 액센트"가 아니라 "상태 정보"이므로 뷰당 액센트 1개 원칙과 충돌하지 않음.

## 다크모드
모든 색 토큰은 light/dark 페어로 정의. ADHD 사용자는 야간에 화면을 오래 보는 경우가 많아 다크모드를 1급 지원 대상으로 취급.

## 스킵한 것 (의도적 단순화)
- 실시간 TickTick 3사 경쟁사 라이브 스캔 — 이미 잘 알려진 패턴이라 생략, 필요시 `/design-system audit --url ...`로 추가 검증 가능.
- 실제 알림/캘린더 동기화 로직 — 이 문서는 시각 디자인 시스템 범위. 알람 스케줄링, EventKit/CalDAV 연동은 별도 구현 단계.
- `ui-skills` 스킬이 요구하는 Tailwind / motion-react / cn / Base UI 등 실제 스택 — 지금 산출물은 정적 HTML 프리뷰라 CSS 변수 + vanilla JS로 동일한 원칙(compositor-only 애니메이션, 뷰당 액센트 1개, 접근 가능한 포커스 상태 등)만 적용. 실제 React 앱으로 넘어갈 때 이 스택 규칙을 그대로 적용.
